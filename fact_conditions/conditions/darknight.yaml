name: darknight
description: Dark night condition set used by erna
conditions: [
    fRunTypeName = "data",
    fZenithDistanceMean < 30,
    fTriggerRateMedian > 40,
    fTriggerRateMedian < 85,
    fEffectiveOn > 0.95,
    fCurrentsMedMean < 20,
    fMoonZenithDistance > 100,
]